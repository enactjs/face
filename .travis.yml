language: node_js
node_js:
    - "node"
cache:
  directories:
    - ~/npm_cache
sudo: false
install:
    - npm config set prefer-offline true
    - npm install -g enactjs/cli#develop
    - npm install
script:
    - enact lint --strict
    - npm run pack

